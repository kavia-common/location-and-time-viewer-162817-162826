This directory mirrors Gradle wrapper metadata so CI tools that expect 'gradle/wrapper' alongside the root './gradlew' can locate wrapper files.

The actual wrapper JAR and properties are located under:
  android_frontend/gradle/wrapper/

Root './gradlew' delegates to 'android_frontend/gradlew'.
